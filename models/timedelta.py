

def high(t):
    return {'d': t.days, 's': t.seconds, 'ms': t.milliseconds, 'm': t.minutes, 'h': t.hours, 'w': t.weeks}


def mid(t):
    return {'d': t.days, 's': t.seconds, 'ms': t.milliseconds, 'm': t.minutes, 'h': t.hours, 'w': t.weeks}


def low(t):
    return {'d': t.days, 's': t.seconds, 'ms': t.milliseconds, 'm': t.minutes, 'h': t.hours, 'w': t.weeks}
